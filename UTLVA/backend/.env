SECRET_KEY=django-insecure-si(%ebj4882mviilp6uk-3@ivf=bwgr1&pdc@fr(9&sn6^dcpx
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

DB_NAME=utlva_db
DB_USER=utlva_user
DB_PASSWORD=UTLVA_dev_2025
DB_HOST=localhost
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8080

ACCESS_TOKEN_LIFETIME_MINUTES=60
REFRESH_TOKEN_LIFETIME_DAYS=7

# ── Email (Gmail SMTP via App Password) ──────────────────────────────────────
# App passwords contain spaces — they MUST be quoted here so python-decouple
# reads the full string including spaces.
# TLS=True + SSL=False + port 587 is the correct Gmail STARTTLS config.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=maduhumagugwani@gmail.com
EMAIL_HOST_PASSWORD="vmjb wpjc sspy mbab"
DEFAULT_FROM_EMAIL=maduhumagugwani@gmail.com

# Frontend URL for password-reset link in emails
FRONTEND_URL=http://localhost:46063

# ── Notification channels ─────────────────────────────────────────────────────
# SMS Gateway: 'stub' (console-only, default) | 'africastalking' (live)
SMS_GATEWAY=stub
# Set these when switching to africastalking:
# AT_USERNAME=sandbox
# AT_API_KEY=your_api_key_here

# SMS caps (FR-51-B)
SMS_DAILY_CAP_PER_USER=5
SMS_BULK_APPROVAL_THRESHOLD=50

# FCM Push: 'stub' (console-only, default) | 'firebase' (live)
PUSH_GATEWAY=stub
# Set this when switching to firebase:
# FCM_CREDENTIALS_PATH=serviceAccountKey.json
